# 框架模块下沉热更（极简启动器重构）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 AOT 框架模块尽可能下沉到单一 `Hotfix` 程序集，使模块系统与绝大多数框架模块可热更，AOT 仅保留「下载资源 + 加载 DLL + 显示进度」的最小启动器。

**Architecture:** AOT 侧新增自包含引导（`BootstrapProcess` 线性异步流程 + `BootstrapAssetHelper` 直连 YooAsset + `BootstrapView` 基于 Resources 的 FairyGUI 加载界面），替代原有 9 个 `ProcedureXxx` 流程链与 `WinLauncher`。随后按逆依赖序把 11 个框架模块及模块系统本体（`ModuleManager`/`ModuleBase`/`GlobalModule`）迁入 Hotfix 程序集，模块沿用原命名空间以实现调用点零改动；最后把帧驱动更新循环从 AOT 移交给热更侧模块系统。

**Tech Stack:** Unity 2022.3.33f1c1、HybridCLR、YooAsset 2.3.18、FairyGUI、UniTask、C#（asmdef 程序集）。

## Global Constraints

- 全程使用中文注释与提交信息；提交遵循 Conventional Commits 中文版；AI 生成提交在标题后追加 `[AI]`。
- HybridCLR 引用规则：热更层可引用 AOT，AOT 绝不能引用热更层。
- 热更程序集只有 `Hotfix` 一个（guid `00a5234a2895a5544965dce970e4e128`）；下沉模块一律并入该程序集，不新增热更程序集。
- 迁入 Hotfix 的模块**沿用原命名空间**（如 `FuFramework.UI.Runtime`），禁止改命名空间——保证所有调用点 `using` 与类型引用零改动。
- `ModuleBase` 已是纯 C# 类；模块经 `Activator.CreateInstance` 反射创建，不参与场景/预制体序列化。
- `ModuleSetting`（ScriptableObject 体系）与 `FuLogger` 留在 AOT，不热更。
- 无自动化测试。每个任务的验证 = ①Unity 编辑器编译无报错（切到 Unity 窗口等待域重载，检查 Console）②关键任务额外在 Play 模式（`EditorSimulateMode`）确认进入登录界面。Unity 由开发者运行。
- 当前工作分支：`refactor/framework-modules-to-hotfix`。

---

## 文件结构总览

### AOT 侧新增（`Scripts/AOT/Bootstrap/`）
- `IBootstrapView.cs` → 放入 `Core/Runtime/Base/`（AOT，供热更侧引用的加载界面句柄接口）
- `BootstrapContext.cs` → 放入 `Core/Runtime/Base/`（AOT，YooAsset 就绪状态跨程序集共享）
- `Scripts/AOT/Bootstrap/BootstrapView.cs`（AOT，Resources FairyGUI 加载界面）
- `Scripts/AOT/Bootstrap/BootstrapAssetHelper.cs`（AOT，YooAsset 引导封装）
- `Scripts/AOT/Bootstrap/BootstrapProcess.cs`（AOT，线性引导流程）

### AOT 侧修改
- `Scripts/FuFramework/Launcher/Runtime/Launcher.cs`、`Launcher.Modules.cs`（瘦身 + 委托驱动）
- `Scripts/Hotfix/HotfixLauncher.cs`（接收 `IBootstrapView`、承接模块注册与更新循环移交）
- `Scripts/FuFramework/Assets/Runtime/AssetModule.cs`（`OnInit` 复用 YooAsset 初始化）

### AOT 侧删除
- `Scripts/AOT/Procedure/Procedure*.cs`（9 个流程类，`ProcedureCodeHotfix` 逻辑并入 `BootstrapProcess`）
- `Scripts/AOT/UI/Launcher/`（旧 `WinLauncher` ViewBase 版；`.Gen` 由 FGUI 生成，保留 Resources 中的 FUI 资源本体）

### 迁移（AOT → `Scripts/Hotfix/Framework/<模块>/`）
ReferencePool、ObjectPool、FSM、Procedure、Event、Mono、Timer、Asset、UI、TaskPool、Variable，及 `Core/Runtime/Framework/{ModuleManager,ModuleBase}`、`Launcher/Runtime/GlobalModule.cs`

---

## Phase 0 · 基线确认

### Task 0：确认可运行基线

**Files:** 无改动

- [ ] **Step 1: 确认当前分支与工作区干净**

Run: `git status && git branch --show-current`
Expected: 分支为 `refactor/framework-modules-to-hotfix`，工作区 clean。

- [ ] **Step 2: 在 Unity 编辑器验证基线可运行**

打开 Unity 工程，确认 `AssetSetting.PlayMode = EditorSimulateMode`，进入 Play 模式，确认能走完加载界面并进入登录界面。记录加载界面外观（进度条、提示文字、更新框）作为回归基准。
Expected: 正常进入登录界面，Console 无报错。

- [ ] **Step 3: 记录基线**

无需提交。此任务仅确认基线，后续每个任务都以「编译无报错 + 进入登录界面」为回归标准。

---

## Phase 1 · AOT 引导重写（模块系统原地不动）

### Task 1：新增 AOT 跨程序集契约（IBootstrapView + BootstrapContext）

**Files:**
- Create: `Scripts/FuFramework/Core/Runtime/Base/IBootstrapView.cs`
- Create: `Scripts/FuFramework/Core/Runtime/Base/BootstrapContext.cs`

**Interfaces:**
- Produces: `FuFramework.Core.Runtime.IBootstrapView { void SetTip(string text); void Close(); }`；`FuFramework.Core.Runtime.BootstrapContext { static bool YooAssetInitialized; static string DefaultPackageName; }`

- [ ] **Step 1: 编写 IBootstrapView**

```csharp
// Scripts/FuFramework/Core/Runtime/Base/IBootstrapView.cs
// ReSharper disable once CheckNamespace
namespace FuFramework.Core.Runtime
{
    /// <summary>
    /// 启动加载界面句柄接口。
    /// 由 AOT 的 BootstrapView 实现，经 HotfixLauncher.MainAsync 传入热更侧，
    /// 使加载界面能跨越「下载(AOT)→加载配置/UI(热更)」两阶段并在登录界面打开时关闭。
    /// </summary>
    public interface IBootstrapView
    {
        /// <summary>设置加载提示文本。</summary>
        void SetTip(string text);

        /// <summary>关闭加载界面。</summary>
        void Close();
    }
}
```

- [ ] **Step 2: 编写 BootstrapContext**

```csharp
// Scripts/FuFramework/Core/Runtime/Base/BootstrapContext.cs
// ReSharper disable once CheckNamespace
namespace FuFramework.Core.Runtime
{
    /// <summary>
    /// 启动引导共享上下文。
    /// 功能：由 AOT 引导助手在初始化 YooAsset 后写入状态，供热更侧 AssetModule 判断是否复用，避免二次初始化。
    /// </summary>
    public static class BootstrapContext
    {
        /// <summary>YooAsset 是否已由引导助手完成初始化。</summary>
        public static bool YooAssetInitialized { get; set; }

        /// <summary>默认资源包名称（引导期写入）。</summary>
        public static string DefaultPackageName { get; set; }
    }
}
```

- [ ] **Step 3: Unity 编译验证**

切到 Unity 窗口等待域重载。
Expected: Console 无报错（新增两个纯 C# 文件，Core 程序集编译通过）。

- [ ] **Step 4: Commit**

```bash
git add "Scripts/FuFramework/Core/Runtime/Base/IBootstrapView.cs" "Scripts/FuFramework/Core/Runtime/Base/BootstrapContext.cs"
git commit -m "feat: 新增 AOT 引导契约 IBootstrapView 与 BootstrapContext [AI]"
```

---

### Task 2：新增 AOT 加载界面 BootstrapView（Resources FairyGUI）

**Files:**
- Create: `Scripts/AOT/Bootstrap/BootstrapView.cs`

**Interfaces:**
- Consumes: `FuFramework.Core.Runtime.IBootstrapView`
- Produces: `Launcher.BootstrapView : IBootstrapView`，成员：`static UniTask<BootstrapView> CreateAsync()`、`void SetTip(string)`、`void SetProgress(float value01, string tip)`、`void ShowUpdateDialog(string content, System.Action onConfirm)`、`void SetNeedUpgrade(bool)`、`void SetDownloading(bool)`、`void Close()`

**背景：** "Launcher" FUI 包位于 `Resources/UI/Launcher/`，通过 `UIPackage.AddPackage("UI/Launcher/Launcher")` 从 Resources 加载，不依赖 YooAsset/UIModule。组件结构（见旧 `WinLauncher.Gen.cs`）：控制器 `IsNeedUpgrade`/`IsDownloading`，子元件 `_txtTips`(GTextField)、`_progressBar`(GProgressBar)、`_btnOk`(GButton)、`_txtContent`(GRichTextField)。

- [ ] **Step 1: 编写 BootstrapView**

```csharp
// Scripts/AOT/Bootstrap/BootstrapView.cs
using System;
using FairyGUI;
using Cysharp.Threading.Tasks;
using FuFramework.Core.Runtime;

// ReSharper disable once CheckNamespace
namespace Launcher
{
    /// <summary>
    /// AOT 启动加载界面。
    /// 功能：从 Resources 加载 "Launcher" FUI 包并显示进度/提示/更新确认框，脱离 UIModule/EventModule 自包含运行。
    /// </summary>
    public sealed class BootstrapView : IBootstrapView
    {
        private GComponent m_View;
        private Controller m_IsNeedUpgrade;
        private Controller m_IsDownloading;
        private GTextField m_TxtTips;
        private GProgressBar m_ProgressBar;
        private GButton m_BtnOk;
        private GRichTextField m_TxtContent;
        private Action m_OnConfirm;

        /// <summary>创建并显示加载界面。</summary>
        public static UniTask<BootstrapView> CreateAsync()
        {
            var view = new BootstrapView();
            view.Init();
            return UniTask.FromResult(view);
        }

        private void Init()
        {
            // 从 Resources 加载 Launcher 包（不依赖 YooAsset）
            UIPackage.AddPackage("UI/Launcher/Launcher");

            m_View = UIPackage.CreateObject("Launcher", "WinLauncher").asCom;
            m_View.MakeFullScreen();
            GRoot.inst.AddChild(m_View);

            m_IsNeedUpgrade = m_View.GetController("IsNeedUpgrade");
            m_IsDownloading = m_View.GetController("IsDownloading");
            m_TxtTips       = (GTextField)m_View.GetChild("_txtTips");
            m_ProgressBar   = (GProgressBar)m_View.GetChild("_progressBar");
            m_BtnOk         = (GButton)m_View.GetChild("_btnOk");
            m_TxtContent    = (GRichTextField)m_View.GetChild("_txtContent");

            m_BtnOk.onClick.Set(OnBtnOkClick);
            SetNeedUpgrade(false);
            SetDownloading(false);
        }

        /// <summary>设置提示文本。</summary>
        public void SetTip(string text)
        {
            if (m_TxtTips != null) m_TxtTips.text = text;
        }

        /// <summary>设置下载进度（0~1）与提示。</summary>
        public void SetProgress(float value01, string tip)
        {
            SetDownloading(true);
            if (m_ProgressBar != null) m_ProgressBar.value = value01 * 100f;
            SetTip(tip);
        }

        /// <summary>显示更新确认框。</summary>
        public void ShowUpdateDialog(string content, Action onConfirm)
        {
            SetNeedUpgrade(true);
            m_BtnOk.title    = "更新";
            m_TxtContent.text = content;
            m_TxtContent.onClick.Set(ctx =>
            {
                if (ctx.data != null) Utility.Application.OpenURL(ctx.data.ToString());
            });
            m_OnConfirm = onConfirm;
        }

        /// <summary>设置是否显示更新确认框。</summary>
        public void SetNeedUpgrade(bool need) => m_IsNeedUpgrade.SetSelectedIndex(need ? 1 : 0);

        /// <summary>设置是否处于下载中状态。</summary>
        public void SetDownloading(bool downloading) => m_IsDownloading.SetSelectedIndex(downloading ? 1 : 0);

        /// <summary>关闭并销毁加载界面。</summary>
        public void Close()
        {
            if (m_View == null) return;
            GRoot.inst.RemoveChild(m_View, true);
            m_View = null;
        }

        private void OnBtnOkClick(EventContext ctx) => m_OnConfirm?.Invoke();
    }
}
```

- [ ] **Step 2: Unity 编译验证**

切到 Unity 窗口等待域重载。
Expected: Console 无报错。（`BootstrapView` 位于 `AOT` 程序集，已引用 FairyGUI、UniTask、Core。）

> 说明：此时 `BootstrapView` 尚未被调用，仅验证编译。运行验证在 Task 5 完成整体接线后进行。

- [ ] **Step 3: Commit**

```bash
git add "Scripts/AOT/Bootstrap/BootstrapView.cs"
git commit -m "feat: 新增 AOT 自包含加载界面 BootstrapView [AI]"
```

---

### Task 3：新增 AOT 资源引导助手 BootstrapAssetHelper

**Files:**
- Create: `Scripts/AOT/Bootstrap/BootstrapAssetHelper.cs`

**Interfaces:**
- Consumes: `FuFramework.Core.Runtime.BootstrapContext`、`FuFramework.ModuleSetting.Runtime.ModuleSetting`、YooAsset API
- Produces: `Launcher.BootstrapAssetHelper` 静态类，成员：`static EPlayMode PlayMode`、`static string DefaultPackageName`、`static UniTask InitPackageAsync(string url=null, string backupUrl=null)`、`static ResourcePackage DefaultPackage`、`static UniTask<string> RequestVersionAsync()`、`static UniTask<bool> UpdateManifestAsync(string version)`、`static ResourceDownloaderOperation CreateDownloader()`、`static UniTask<byte[]> LoadRawFileBytesAsync(string location)`

**背景：** 移植 `AssetModule.Initialization.cs` 的四种运行模式初始化逻辑；参数来自 `ModuleSetting.Instance.AssetSetting`。

- [ ] **Step 1: 编写 BootstrapAssetHelper**

```csharp
// Scripts/AOT/Bootstrap/BootstrapAssetHelper.cs
using YooAsset;
using Cysharp.Threading.Tasks;
using FuFramework.Core.Runtime;
using FuFramework.ModuleSetting.Runtime;

// ReSharper disable once CheckNamespace
namespace Launcher
{
    /// <summary>
    /// AOT 资源引导助手。
    /// 功能：引导期直连 YooAsset 完成「初始化默认包 / 请求版本号 / 更新清单 / 创建下载器 / 加载原始字节」，
    ///      不依赖 AssetModule。初始化完成后写入 BootstrapContext 供热更侧 AssetModule 复用。
    /// </summary>
    public static class BootstrapAssetHelper
    {
        private static ResourcePackage s_DefaultPackage;
        private static int s_DownloadingMaxNum;
        private static int s_FailedTryAgainNum;

        /// <summary>资源运行模式。</summary>
        public static EPlayMode PlayMode { get; private set; }

        /// <summary>默认资源包名称。</summary>
        public static string DefaultPackageName { get; private set; }

        /// <summary>默认资源包。</summary>
        public static ResourcePackage DefaultPackage => s_DefaultPackage;

        /// <summary>初始化 YooAsset 与默认资源包。</summary>
        public static async UniTask InitPackageAsync(string url = null, string backupUrl = null)
        {
            var assetSetting = ModuleSetting.Instance.AssetSetting;
            PlayMode            = assetSetting.PlayMode;
            DefaultPackageName  = assetSetting.DefaultPackageName;
            s_DownloadingMaxNum = assetSetting.DownloadingMaxNum;
            s_FailedTryAgainNum = assetSetting.FailedTryAgainNum;

            if (!BootstrapContext.YooAssetInitialized)
            {
                YooAssets.Initialize();
                YooAssets.SetOperationSystemMaxTimeSlice(assetSetting.AsyncSystemMaxSlicePerFrame);
                BootstrapContext.YooAssetInitialized = true;
                BootstrapContext.DefaultPackageName  = DefaultPackageName;
            }

            s_DefaultPackage = YooAssets.TryGetPackage(DefaultPackageName) ?? YooAssets.CreatePackage(DefaultPackageName);
            YooAssets.SetDefaultPackage(s_DefaultPackage);

            var initOp = InitByPlayMode(url, backupUrl);
            await initOp.ToUniTask();
            if (initOp.Status != EOperationStatus.Succeed)
                FuLogger.LogError($"[Bootstrap] 资源包初始化失败: {initOp.Error}");
        }

        private static InitializationOperation InitByPlayMode(string url, string backupUrl)
        {
            switch (PlayMode)
            {
                case EPlayMode.EditorSimulateMode:
                {
                    var buildResult = EditorSimulateModeHelper.SimulateBuild(DefaultPackageName);
                    var fs          = FileSystemParameters.CreateDefaultEditorFileSystemParameters(buildResult.PackageRootDirectory);
                    return s_DefaultPackage.InitializeAsync(new EditorSimulateModeParameters { EditorFileSystemParameters = fs });
                }
                case EPlayMode.OfflinePlayMode:
                {
                    var fs = FileSystemParameters.CreateDefaultBuildinFileSystemParameters();
                    return s_DefaultPackage.InitializeAsync(new OfflinePlayModeParameters { BuildinFileSystemParameters = fs });
                }
                case EPlayMode.HostPlayMode:
                {
                    IRemoteServices remote = new RemoteServices(url, backupUrl);
                    var cacheFs   = FileSystemParameters.CreateDefaultCacheFileSystemParameters(remote);
                    var buildInFs = FileSystemParameters.CreateDefaultBuildinFileSystemParameters();
                    return s_DefaultPackage.InitializeAsync(new HostPlayModeParameters { BuildinFileSystemParameters = buildInFs, CacheFileSystemParameters = cacheFs });
                }
                case EPlayMode.WebPlayMode:
                {
                    var webFs = FileSystemParameters.CreateDefaultWebServerFileSystemParameters();
                    return s_DefaultPackage.InitializeAsync(new WebPlayModeParameters { WebServerFileSystemParameters = webFs });
                }
                default:
                    return null;
            }
        }

        /// <summary>请求资源版本号，失败返回 null。</summary>
        public static async UniTask<string> RequestVersionAsync()
        {
            var op = s_DefaultPackage.RequestPackageVersionAsync();
            await op.ToUniTask();
            return op.Status == EOperationStatus.Succeed ? op.PackageVersion : null;
        }

        /// <summary>更新资源清单，返回是否成功。</summary>
        public static async UniTask<bool> UpdateManifestAsync(string version)
        {
            var op = s_DefaultPackage.UpdatePackageManifestAsync(version);
            await op.ToUniTask();
            return op.Status == EOperationStatus.Succeed;
        }

        /// <summary>创建资源下载器。</summary>
        public static ResourceDownloaderOperation CreateDownloader()
            => s_DefaultPackage.CreateResourceDownloader(s_DownloadingMaxNum, s_FailedTryAgainNum);

        /// <summary>加载原始文件字节（用于 DLL/AOT 元数据）。</summary>
        public static async UniTask<byte[]> LoadRawFileBytesAsync(string location)
        {
            var handle = s_DefaultPackage.LoadRawFileAsync(location);
            await handle.ToUniTask();
            return handle.GetRawFileData();
        }
    }
}
```

- [ ] **Step 2: 校验 YooAsset 2.3.18 API 名称**

在 Unity 中确认以下 API 存在且签名匹配（YooAsset 2.3.18）：`YooAssets.TryGetPackage`、`YooAssets.SetDefaultPackage`、`ResourcePackage.LoadRawFileAsync(string)`、`RawFileHandle.GetRawFileData()`、`InitializationOperation.ToUniTask()`（若无 `ToUniTask`，改用 `await op;` 或 `.Task`）。如有差异按实际 API 调整。
Expected: 编译无报错。

- [ ] **Step 3: Unity 编译验证**

切到 Unity 窗口等待域重载。
Expected: Console 无报错。

- [ ] **Step 4: Commit**

```bash
git add "Scripts/AOT/Bootstrap/BootstrapAssetHelper.cs"
git commit -m "feat: 新增 AOT 资源引导助手 BootstrapAssetHelper [AI]"
```

---

### Task 4：新增 AOT 引导流程 BootstrapProcess

**Files:**
- Create: `Scripts/AOT/Bootstrap/BootstrapProcess.cs`

**Interfaces:**
- Consumes: `Launcher.BootstrapView`、`Launcher.BootstrapAssetHelper`、`Launcher.Procedure.RemoteUpdateConfig`、`AOTGenericReferences`、`FuFramework.Core.Runtime.Utility`
- Produces: `Launcher.BootstrapProcess { static UniTask RunAsync(System.Func<BootstrapView, Cysharp.Threading.Tasks.UniTask> onHotfixEntry) }`

**背景：** 把原 9 个流程类的逻辑（`ProcedureLauncher`→…→`ProcedureCodeHotfix`）合并为一条线性异步流程。远端配置字段见 `RemoteUpdateConfig`；DLL/AOT 路径用 `Utility.AssetPath.GetCodePath/GetAOTCodePath`；热更程序集名 `"Hotfix"`。`onHotfixEntry` 由 Launcher 传入，负责反射调用 `HotfixLauncher.MainAsync(view)`。

- [ ] **Step 1: 编写 BootstrapProcess**

```csharp
// Scripts/AOT/Bootstrap/BootstrapProcess.cs
using System;
using UnityEngine;
using UnityEngine.Networking;
using Cysharp.Threading.Tasks;
using YooAsset;
using HybridCLR;
using FuFramework.Core.Runtime;
using FuFramework.ModuleSetting.Runtime;
using Launcher.Procedure;
using Utility = FuFramework.Core.Runtime.Utility;

// ReSharper disable once CheckNamespace
namespace Launcher
{
    /// <summary>
    /// AOT 启动引导流程。
    /// 功能：显示加载界面 → 资源更新（远端配置/版本/清单/下载）→ 加载 AOT 元数据与 Hotfix.dll → 移交热更入口。
    /// </summary>
    public static class BootstrapProcess
    {
        private const string RemoteUpdateConfigName = "RemoteUpdateConfig.json";
        private const string HotfixDllName = "Hotfix";

        private static BootstrapView s_View;

        /// <summary>运行引导流程。onHotfixEntry：加载完 Hotfix 程序集后由外部执行热更入口调用。</summary>
        public static async UniTask RunAsync(Func<BootstrapView, UniTask> onHotfixEntry)
        {
            FuLogger.LogInfo("<color=#43f656>------进入启动引导流程------</color>");
            s_View = await BootstrapView.CreateAsync();

            var playMode = ModuleSetting.Instance.AssetSetting.PlayMode;
            RemoteUpdateConfig updateConfig = null;

            // 联机/Web 模式：请求远端更新配置（含强更判断）
            if (playMode is not (EPlayMode.EditorSimulateMode or EPlayMode.OfflinePlayMode))
            {
                updateConfig = await ReqRemoteUpdateConfigWithRetry();
                if (updateConfig.ForceUpdate)
                {
                    s_View.ShowUpdateDialog(updateConfig.UpdateAnnouncement, () => Application.OpenURL(updateConfig.AppDownloadUrl));
                    return; // 强更中止后续流程
                }
            }

            // 初始化资源包
            s_View.SetTip("InitPackage...");
            if (updateConfig == null)
            {
                await BootstrapAssetHelper.InitPackageAsync();
            }
            else
            {
                var version   = $"v{Utility.Version.MajorMinorVersion}";
                var url       = string.Format(updateConfig.ResDownloadUrl, version);
                var backupUrl = string.Format(updateConfig.ResDownloadBackupUrl, version);
                await BootstrapAssetHelper.InitPackageAsync(url, backupUrl);
            }

            // 获取版本号（失败重试）
            s_View.SetTip("GetVersion...");
            string packageVersion;
            while ((packageVersion = await BootstrapAssetHelper.RequestVersionAsync()) == null)
            {
                s_View.SetTip("获取版本号失败，正在重试...");
                await UniTask.WaitForSeconds(3);
            }

            // 更新资源清单（失败重试）
            s_View.SetTip("UpdateManifest...");
            while (!await BootstrapAssetHelper.UpdateManifestAsync(packageVersion))
            {
                s_View.SetTip("更新清单失败，正在重试...");
                await UniTask.WaitForSeconds(3);
            }

            // 联机模式：创建下载器并下载（编辑器/离线跳过）
            if (playMode is not (EPlayMode.EditorSimulateMode or EPlayMode.OfflinePlayMode))
                await CreateAndDownload(updateConfig);

            // 资源更新完毕
            s_View.SetDownloading(false);
            s_View.SetTip(string.Empty);

            // 加载 AOT 补充元数据 + Hotfix.dll，移交热更入口
            await LoadHotfixAndHandoff(onHotfixEntry);
        }

        /// <summary>请求远端更新配置（失败重试）。</summary>
        private static async UniTask<RemoteUpdateConfig> ReqRemoteUpdateConfigWithRetry()
        {
            var assetSetting = ModuleSetting.Instance.AssetSetting;
            var configUrl    = $"{assetSetting.ResCdnRootRootURL}{Utility.Application.PlatformName}/{RemoteUpdateConfigName}";
            while (true)
            {
                try
                {
                    using var request = UnityWebRequest.Get(configUrl);
                    request.timeout = 5;
                    await request.SendWebRequest();
                    if (request.result == UnityWebRequest.Result.Success)
                    {
                        var cfg = Utility.Json.ToObject<RemoteUpdateConfig>(request.downloadHandler.text);
                        if (cfg != null) return cfg;
                    }
                }
                catch (Exception e)
                {
                    FuLogger.LogError($"[Bootstrap] 获取远端更新配置异常：{e.Message}");
                }
                s_View.SetTip("资源服务器错误，正在重试...");
                await UniTask.WaitForSeconds(3);
            }
        }

        /// <summary>创建下载器并下载资源。</summary>
        private static async UniTask CreateAndDownload(RemoteUpdateConfig updateConfig)
        {
            while (true)
            {
                var downloader = BootstrapAssetHelper.CreateDownloader();
                if (downloader.TotalDownloadCount == 0) return; // 无需下载

                // 需要确认更新
                if (updateConfig is { ShowUpdateTips: true })
                {
                    var confirmed = new UniTaskCompletionSource();
                    s_View.ShowUpdateDialog(updateConfig.UpdateAnnouncement, () =>
                    {
                        s_View.SetNeedUpgrade(false);
                        confirmed.TrySetResult();
                    });
                    await confirmed.Task;
                }

                var totalBytes = downloader.TotalDownloadBytes;
                downloader.DownloadUpdateCallback = data =>
                {
                    var progress = data.CurrentDownloadBytes / (data.TotalDownloadBytes * 1f);
                    var cur = Utility.File.GetBytesSizeWithUnit(data.CurrentDownloadBytes);
                    var tot = Utility.File.GetBytesSizeWithUnit(data.TotalDownloadBytes);
                    s_View.SetProgress(progress, $"下载中：{cur}/{tot}");
                };
                var failed = false;
                downloader.DownloadErrorCallback = _ => failed = true;

                downloader.BeginDownload();
                await downloader.ToUniTask();

                if (!failed && downloader.Status == EOperationStatus.Succeed) return; // 下载成功
                s_View.SetTip("下载失败，正在重试...");
                await UniTask.WaitForSeconds(3); // 失败后重建下载器重试
            }
        }

        /// <summary>加载 AOT 补充元数据与 Hotfix 程序集，并移交热更入口。</summary>
        private static async UniTask LoadHotfixAndHandoff(Func<BootstrapView, UniTask> onHotfixEntry)
        {
            FuLogger.LogInfo("<color=#43f656>------进入代码热更流程------</color>");

            // 编辑器模拟模式：程序集已在域中，直接移交
            if (Utility.Application.IsEditor && BootstrapAssetHelper.PlayMode == EPlayMode.EditorSimulateMode)
            {
                await onHotfixEntry(s_View);
                return;
            }

            // 补充 AOT 元数据
            foreach (var aotDll in AOTGenericReferences.PatchedAOTAssemblyList)
            {
                var bytes = await BootstrapAssetHelper.LoadRawFileBytesAsync(Utility.AssetPath.GetAOTCodePath(aotDll));
                RuntimeApi.LoadMetadataForAOTAssembly(bytes, HomologousImageMode.SuperSet);
                FuLogger.LogInfo($"[Bootstrap] 补充 AOT 元数据：{aotDll}");
            }

            // 加载 Hotfix.dll（域内自动注册程序集，供 onHotfixEntry 反射调用入口）
            var dllBytes = await BootstrapAssetHelper.LoadRawFileBytesAsync(Utility.AssetPath.GetCodePath($"{HotfixDllName}.dll"));
            System.Reflection.Assembly.Load(dllBytes);
            FuLogger.LogInfo("[Bootstrap] Hotfix 程序集加载完成");

            await onHotfixEntry(s_View);
        }
    }
}
```

- [ ] **Step 2: 校验 API 名称**

确认 `RemoteUpdateConfig` 字段名与旧流程一致：`ForceUpdate`、`AppDownloadUrl`、`UpdateAnnouncement`、`ResDownloadUrl`、`ResDownloadBackupUrl`、`ShowUpdateTips`（见 `Scripts/AOT/Procedure/UpdateConfig/RemoteUpdateConfig.cs`）。确认 `ResourceDownloaderOperation.ToUniTask()` 可用（否则改 `await downloader;`）。
Expected: 名称匹配。

- [ ] **Step 3: Unity 编译验证**

切到 Unity 窗口等待域重载。
Expected: Console 无报错。

- [ ] **Step 4: Commit**

```bash
git add "Scripts/AOT/Bootstrap/BootstrapProcess.cs"
git commit -m "feat: 新增 AOT 线性引导流程 BootstrapProcess [AI]"
```

---

### Task 5：接线 Launcher/HotfixLauncher/AssetModule 并删除旧引导

**Files:**
- Modify: `Scripts/FuFramework/Launcher/Runtime/Launcher.cs`
- Modify: `Scripts/FuFramework/Launcher/Runtime/Launcher.Modules.cs`
- Delete: `Scripts/FuFramework/Launcher/Runtime/Launcher.Procedures.cs`
- Modify: `Scripts/Hotfix/HotfixLauncher.cs`
- Modify: `Scripts/FuFramework/Assets/Runtime/AssetModule.cs`
- Delete: `Scripts/AOT/Procedure/Procedure*.cs`（9 个流程类）
- Delete: `Scripts/AOT/UI/Launcher/Impl/WinLauncher.cs`、`Scripts/AOT/UI/Launcher/Gen/WinLauncher.Gen.cs`

**Interfaces:**
- Consumes: `Launcher.BootstrapProcess.RunAsync`、`FuFramework.Core.Runtime.IBootstrapView`
- Produces: `Launcher.Launcher.OnUpdate/OnLateUpdate/OnFixedUpdate`（`System.Action<float,float>`/`System.Action` 委托）；`HotfixLauncher.MainAsync(IBootstrapView)`

- [ ] **Step 1: 改造 Launcher.cs——启动引导 + 委托驱动**

将 `Start()`、`Update/LateUpdate/FixedUpdate` 改为：

```csharp
// Scripts/FuFramework/Launcher/Runtime/Launcher.cs （替换 Start 与三个驱动方法）
using System;
using System.Reflection;
using UnityEngine;
using Cysharp.Threading.Tasks;
using FuFramework.Core.Runtime;

// ...类体内：

        /// <summary>帧更新委托（移交后由热更侧 ModuleManager 驱动）。</summary>
        public static Action<float, float> OnUpdate;
        public static Action<float, float> OnLateUpdate;
        public static Action OnFixedUpdate;

        private void Start()
        {
            // 启动 AOT 引导流程；加载完 Hotfix 后反射调用热更入口
            BootstrapProcess.RunAsync(InvokeHotfixEntryAsync).Forget();
        }

        /// <summary>反射调用热更入口 HotfixLauncher.MainAsync(IBootstrapView)。</summary>
        private static async UniTask InvokeHotfixEntryAsync(BootstrapView view)
        {
            var assembly   = GetHotfixAssembly();
            var entryType  = assembly.GetType("Hotfix.HotfixLauncher");
            var mainMethod = entryType.GetMethod("MainAsync");
            var result     = mainMethod.Invoke(null, new object[] { view });
            if (result is UniTask task) await task;
            else FuLogger.LogError("[Launcher] 热更入口不是可等待的 UniTask");
        }

        private static Assembly GetHotfixAssembly()
        {
            foreach (var asm in Utility.Assembly.GetAssemblies())
            {
                if (asm.GetName().Name.Equals("Hotfix", StringComparison.OrdinalIgnoreCase))
                    return asm;
            }
            return null;
        }

        private void Update()      => OnUpdate?.Invoke(Time.deltaTime, Time.unscaledDeltaTime);
        private void LateUpdate()  => OnLateUpdate?.Invoke(Time.deltaTime, Time.unscaledDeltaTime);
        private void FixedUpdate() => OnFixedUpdate?.Invoke();
```

> 注意：Phase 1 阶段 `ModuleManager` 仍在 AOT，但更新循环已改为委托。为使 Phase 1 能运行，在本步骤末尾于 `InvokeHotfixEntryAsync` 调用前，把委托指向 `ModuleManager`：`OnUpdate = ModuleManager.Update; OnLateUpdate = ModuleManager.LateUpdate; OnFixedUpdate = ModuleManager.FixedUpdate;`（Phase 3 移交给热更侧后删除此临时赋值）。同时保留 `OnInit()` 中的日志逻辑不变。

- [ ] **Step 2: 改造 Launcher.Modules.cs——注册时机改到引导后**

`RegisterModules()` 保留 9 个基础模块注册，但**不再由 `Start()` 调用**；改为由 `InvokeHotfixEntryAsync` 在调用热更入口前调用一次（此时 YooAsset 已由 `BootstrapAssetHelper` 初始化，`AssetModule.OnInit` 复用）。在 `Launcher.cs` 的 `InvokeHotfixEntryAsync` 开头加入：

```csharp
            RegisterModules();       // 注册 9 个 AOT 基础模块（Phase 2 会逐步迁往 HotfixLauncher）
            OnUpdate      = ModuleManager.Update;
            OnLateUpdate  = ModuleManager.LateUpdate;
            OnFixedUpdate = ModuleManager.FixedUpdate;
```

（`RegisterModules` 是 `Launcher` 的 partial 方法，`InvokeHotfixEntryAsync` 为 static，需将 `RegisterModules` 改为通过 `Instance.RegisterModules()` 调用，或将其改为 static 方法。采用 `Instance.RegisterModules()`。）

- [ ] **Step 3: 删除旧流程编排 Launcher.Procedures.cs**

原 `StartProcedure()` 及 Inspector 扫描的流程编排不再需要（引导已线性化）。删除文件：

```bash
git rm "Scripts/FuFramework/Launcher/Runtime/Launcher.Procedures.cs"
```

> 若 `Launcher.Procedures.cs` 中含 Inspector 面板依赖的字段（流程列表），需同步清理 `Launcher/Editor/Inspector/LauncherInspector.cs` 中对应引用。检查并移除相关代码。

- [ ] **Step 4: 改造 HotfixLauncher.MainAsync 接收 IBootstrapView**

```csharp
// Scripts/Hotfix/HotfixLauncher.cs （改签名 + 用 view 驱动加载提示，替换原 WinLauncher 相关代码）
        public static async UniTask MainAsync(IBootstrapView bootstrapView)
        {
            FuLogger.LogInfo("<color=#43f656>------热更逻辑完毕，进入热更后的代码逻辑入口------</color>");

            ProtoMessageIdHandler.Init(HotfixProtoHandler.CurrentAssembly);

            // 注册热更层框架模块（Phase 2/3 会把基础模块注册也并入此处）
            ModuleManager.RegisterModule<RedDotModule>();
            FairyGUI.UIObjectFactory.SetLoaderExtension(typeof(CustomLoader));
            ModuleManager.RegisterModule<GuideModule>();
            ModuleManager.RegisterModule<StorageModule>();
            ModuleManager.RegisterModule<LocalizationModule>();
            ModuleManager.RegisterModule<ModelModule>();
            ModuleManager.RegisterModule<ConfigModule>();
            ModuleManager.RegisterModule<SceneModule>();
            ModuleManager.RegisterModule<SoundModule>();
            ModuleManager.RegisterModule<EntityModule>();
            ModuleManager.RegisterModule<DownloadModule>();
            ModuleManager.RegisterModule<WebModule>();
            ModuleManager.RegisterModule<NetworkModule>();

            bootstrapView.SetTip("LoadConfig...");
            await LoadConfigAsync();

            bootstrapView.SetTip("LoadInitUIAsset...");
            await LoadUIAsync();

            BindCustomComps();
            LocalizationModule.Instance.LocalizationProvider = new LocalizationProvider();

            GlobalModule.UIModule.OpenUI<WinLogin>();

            if (ModuleSetting.Instance.OpenGuide)
            {
                GuideModule.Instance.GuideAction = new GuideActionImpl();
                GuideModule.Instance.StartFirstGuide();
            }

            // 登录界面已打开，关闭 AOT 加载界面
            bootstrapView.Close();
        }
```

在文件顶部 `using` 增加 `using FuFramework.Core.Runtime;`（若未包含），确保 `IBootstrapView` 可见。删除原 `GetUI<WinLauncher>()`/`CloseUI<WinLauncher>()` 相关代码与 `using Launcher.UI;`。

- [ ] **Step 5: 改造 AssetModule.OnInit 复用 YooAsset 初始化**

```csharp
// Scripts/FuFramework/Assets/Runtime/AssetModule.cs （OnInit 内 YooAsset 初始化段替换）
            // 初始化YooAsset（引导助手已初始化则复用，避免二次初始化）
            if (!BootstrapContext.YooAssetInitialized)
            {
                YooAssets.Initialize();
                YooAssets.SetOperationSystemMaxTimeSlice(AsyncSystemMaxSlicePerFrame);
                BootstrapContext.YooAssetInitialized = true;
                BootstrapContext.DefaultPackageName  = DefaultPackageName;
            }
            PackageInited = true;
```

（`BootstrapContext` 在 `FuFramework.Core.Runtime`，`AssetModule` 已 `using FuFramework.Core.Runtime`，无需新增 using。）

- [ ] **Step 6: 删除旧 AOT 流程类与旧 WinLauncher**

```bash
git rm Scripts/AOT/Procedure/ProcedureLauncher.cs Scripts/AOT/Procedure/ProcedureReqRemoteUpdateConfig.cs \
  Scripts/AOT/Procedure/ProcedureInitPackage.cs Scripts/AOT/Procedure/ProcedureGetPackageVersion.cs \
  Scripts/AOT/Procedure/ProcedureUpdatePackageManifest.cs Scripts/AOT/Procedure/ProcedureCreateDownloader.cs \
  Scripts/AOT/Procedure/ProcedureDownloadPackage.cs Scripts/AOT/Procedure/ProcedureUpdateDone.cs \
  Scripts/AOT/Procedure/ProcedureCodeHotfix.cs
git rm "Scripts/AOT/UI/Launcher/Impl/WinLauncher.cs" "Scripts/AOT/UI/Launcher/Gen/WinLauncher.Gen.cs"
```

> 保留 `Scripts/AOT/Procedure/UpdateConfig/RemoteUpdateConfig.cs`（`BootstrapProcess` 仍用）与 `Scripts/AOT/Procedure/Enum`、`Event`（若仍被引用则保留，否则一并清理——用全局搜索确认 `EUpdateStates`/`Asset*EventArgs` 无其它引用后删除）。保留 `Resources/UI/Launcher/` 下 FUI 资源本体（`BootstrapView` 需要）。

- [ ] **Step 7: Unity 编译验证**

切到 Unity 窗口等待域重载，处理所有编译错误（缺失引用、命名空间）。
Expected: Console 无报错。

- [ ] **Step 8: 运行验证（关键回归）**

进入 Play 模式（`EditorSimulateMode`）。
Expected: 出现加载界面（外观同基线）→ 正常进入登录界面 → 加载界面关闭。Console 无报错。

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "refactor: AOT 引导改用极简启动器，移除旧流程链与 WinLauncher [AI]"
```

---

## Phase 2 · 逐个下沉游戏期模块（ModuleManager 暂留 AOT）

> **通用迁移操作**（每个模块任务内均完整执行，勿跨任务引用）：
> 1. 全局搜索确认该模块类型无 AOT 侧引用（`Scripts/AOT/` 与留 AOT 的 Core/ModuleSetting/Launcher）。
> 2. 移动 `Scripts/FuFramework/<模块>/Runtime/` 下所有 `.cs`（连同 `.meta`）到 `Scripts/Hotfix/Framework/<模块>/`。
> 3. 删除该模块 `Runtime.asmdef`（+`.meta`）。
> 4. 若有 `Editor.asmdef`：在 Unity 中打开其 Inspector，从 Assembly Definition References 移除 `FuFramework.<模块>.Runtime`，添加 `Hotfix`；Editor 代码文件保持原位。
> 5. 在 Unity 中打开 **AOT.asmdef** Inspector，从 References 移除 `FuFramework.<模块>.Runtime`。
> 6. 在 Unity 中打开 **Hotfix.asmdef** Inspector，从 References 移除 `FuFramework.<模块>.Runtime`。
> 7. 把该模块的 `ModuleManager.RegisterModule<XxxModule>()` 从 `Launcher.Modules.cs` 迁到 `HotfixLauncher.MainAsync` 顶部（保持相对顺序，见下方各任务指定位置）。
> 8. 编译无报错 + Play 模式进登录界面 + commit。

### Task 6：下沉 UI 模块

**Files:**
- Move: `Scripts/FuFramework/UI/Runtime/*` → `Scripts/Hotfix/Framework/UI/`
- Delete: `Scripts/FuFramework/UI/Runtime/FuFramework.UI.Runtime.asmdef`
- Modify: `Scripts/FuFramework/UI/Editor/FuFramework.UI.Editor.asmdef`（引用改 `Hotfix`）
- Modify: `AOT.asmdef`、`Hotfix.asmdef`（移除 UI.Runtime 引用）
- Modify: `Launcher.Modules.cs`（移除 `RegisterModule<UIModule>()`）、`HotfixLauncher.cs`（新增注册）

- [ ] **Step 1: 确认无 AOT 引用**

Run: `grep -rn "FuFramework.UI.Runtime\|UIModule\|ViewBase" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs`
Expected: 仅 `Launcher.Modules.cs` 的注册行（将迁走）。`BootstrapView` 用的是 FairyGUI 原生 API 而非 UIModule，不应出现。若有其它引用，先解耦。

- [ ] **Step 2: 执行通用迁移操作 1–6**（见上）

移动文件、删 asmdef、改 Editor.asmdef、清 AOT/Hotfix 引用。

- [ ] **Step 3: 迁移注册调用**

从 `Launcher.Modules.cs` 删除 `ModuleManager.RegisterModule<UIModule>();`；在 `HotfixLauncher.MainAsync` 的模块注册区**最前面**插入 `ModuleManager.RegisterModule<UIModule>();`（UI 依赖较多，作为基础层末位，置于 `RedDotModule` 之前）。

- [ ] **Step 4: Unity 编译 + 运行验证**

切 Unity 域重载；进 Play 模式。
Expected: 无报错，进入登录界面（UI 正常）。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor: 下沉 UI 模块到 Hotfix 程序集 [AI]"
```

### Task 7：下沉 Asset 模块

**Files:**
- Move: `Scripts/FuFramework/Assets/Runtime/*` → `Scripts/Hotfix/Framework/Asset/`
- Delete: `FuFramework.Asset.Runtime.asmdef`
- Modify: `Scripts/FuFramework/Assets/Editor/FuFramework.Asset.Editor.asmdef`、`AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`

- [ ] **Step 1: 确认无 AOT 引用**

Run: `grep -rn "FuFramework.Asset.Runtime\|AssetModule" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs`
Expected: 仅注册行。`BootstrapAssetHelper` 用 YooAsset 原生 API，不引用 AssetModule。

- [ ] **Step 2: 执行通用迁移操作 1–6**

- [ ] **Step 3: 迁移注册调用**

`Launcher.Modules.cs` 删 `RegisterModule<AssetModule>()`；`HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<AssetModule>();`（置于 `UIModule` 之前，保持 Asset→UI 顺序）。

- [ ] **Step 4: Unity 编译 + 运行验证**
Expected: 无报错，进入登录界面（资源加载正常，YooAsset 未二次初始化）。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor: 下沉 Asset 模块到 Hotfix 程序集 [AI]"
```

### Task 8：下沉 Timer 模块

**Files:** Move `Scripts/FuFramework/Timer/Runtime/*` → `Scripts/Hotfix/Framework/Timer/`；删 `FuFramework.Timer.Runtime.asmdef`；改 `Timer.Editor.asmdef`、`AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`。

- [ ] **Step 1:** `grep -rn "FuFramework.Timer.Runtime\|TimerModule" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs` → 仅注册行。
- [ ] **Step 2:** 执行通用迁移操作 1–6。
- [ ] **Step 3:** 注册调用迁移：`HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<TimerModule>();`（置于 Asset 之前）。
- [ ] **Step 4:** Unity 编译 + 进登录界面验证。
- [ ] **Step 5:** `git add -A && git commit -m "refactor: 下沉 Timer 模块到 Hotfix 程序集 [AI]"`

### Task 9：下沉 Mono 模块

**Files:** Move `Scripts/FuFramework/Mono/Runtime/*` → `Scripts/Hotfix/Framework/Mono/`；删 `FuFramework.Mono.Runtime.asmdef`；改 `AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`。（Mono 无 Editor.asmdef）

- [ ] **Step 1:** `grep -rn "FuFramework.Mono.Runtime\|MonoModule" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs` → 仅注册行。
- [ ] **Step 2:** 执行通用迁移操作 1–3、5、6。
- [ ] **Step 3:** `HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<MonoModule>();`（置于 Timer 之前）。
- [ ] **Step 4:** Unity 编译 + 进登录界面验证。
- [ ] **Step 5:** `git add -A && git commit -m "refactor: 下沉 Mono 模块到 Hotfix 程序集 [AI]"`

### Task 10：下沉 Event 模块

**Files:** Move `Scripts/FuFramework/Event/Runtime/*` → `Scripts/Hotfix/Framework/Event/`；删 `FuFramework.Event.Runtime.asmdef`；改 `Event.Editor.asmdef`、`AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`。

- [ ] **Step 1:** `grep -rn "FuFramework.Event.Runtime\|EventModule\|GameEventArgs" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs` → 仅注册行（旧 AOT 流程已删，Event 应无 AOT 引用）。
- [ ] **Step 2:** 执行通用迁移操作 1–6。
- [ ] **Step 3:** `HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<EventModule>();`（置于 Mono 之前）。
- [ ] **Step 4:** Unity 编译 + 进登录界面验证。
- [ ] **Step 5:** `git add -A && git commit -m "refactor: 下沉 Event 模块到 Hotfix 程序集 [AI]"`

### Task 11：下沉 Procedure 模块（连带 Variable 检查）

**Files:** Move `Scripts/FuFramework/Procedure/Runtime/*` → `Scripts/Hotfix/Framework/Procedure/`；删 `FuFramework.Procedure.Runtime.asmdef`；改 `AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`。（Procedure 无 Editor.asmdef）

- [ ] **Step 1:** `grep -rn "FuFramework.Procedure.Runtime\|ProcedureBase\|ProcedureModule" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs` → 仅注册行（旧 AOT 流程类已删）。
- [ ] **Step 2:** 执行通用迁移操作 1–3、5、6。
- [ ] **Step 3:** `HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<ProcedureModule>();`（置于 Event 之前）。
- [ ] **Step 4:** Unity 编译 + 进登录界面验证。
- [ ] **Step 5:** `git add -A && git commit -m "refactor: 下沉 Procedure 模块到 Hotfix 程序集 [AI]"`

### Task 12：下沉 FSM 模块

**Files:** Move `Scripts/FuFramework/FSM/Runtime/*` → `Scripts/Hotfix/Framework/FSM/`；删 `FuFramework.FSM.Runtime.asmdef`；改 `FSM.Editor.asmdef`、`AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`。

- [ ] **Step 1:** `grep -rn "FuFramework.Fsm.Runtime\|FsmModule\|FsmBase" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs` → 仅注册行。
- [ ] **Step 2:** 执行通用迁移操作 1–6。
- [ ] **Step 3:** `HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<FsmModule>();`（置于 Procedure 之前）。
- [ ] **Step 4:** Unity 编译 + 进登录界面验证。
- [ ] **Step 5:** `git add -A && git commit -m "refactor: 下沉 FSM 模块到 Hotfix 程序集 [AI]"`

### Task 13：下沉 ObjectPool 模块

**Files:** Move `Scripts/FuFramework/ObjectPool/Runtime/*` → `Scripts/Hotfix/Framework/ObjectPool/`；删 `FuFramework.ObjectPool.Runtime.asmdef`；改 `ObjectPool.Editor.asmdef`、`AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`。

- [ ] **Step 1:** `grep -rn "FuFramework.ObjectPool.Runtime\|ObjectPoolModule" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs` → 仅注册行。
- [ ] **Step 2:** 执行通用迁移操作 1–6。
- [ ] **Step 3:** `HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<ObjectPoolModule>();`（置于 FSM 之前）。
- [ ] **Step 4:** Unity 编译 + 进登录界面验证。
- [ ] **Step 5:** `git add -A && git commit -m "refactor: 下沉 ObjectPool 模块到 Hotfix 程序集 [AI]"`

### Task 14：下沉 ReferencePool、TaskPool、Variable 模块

**Files:**
- Move: `Scripts/FuFramework/ReferencePool/Runtime/*` → `Scripts/Hotfix/Framework/ReferencePool/`
- Move: `Scripts/FuFramework/TaskPool/Runtime/*` → `Scripts/Hotfix/Framework/TaskPool/`
- Move: `Scripts/FuFramework/Variable/*.cs` → `Scripts/Hotfix/Framework/Variable/`
- Delete: 三者的 `Runtime.asmdef`
- Modify: `ReferencePool.Editor.asmdef`、`AOT.asmdef`、`Hotfix.asmdef`、`Launcher.Modules.cs`、`HotfixLauncher.cs`

> ReferencePool 为最底层，最后迁；TaskPool/Variable 无显式注册，随本任务一并迁移。

- [ ] **Step 1:** 确认无 AOT 引用：
Run: `grep -rn "FuFramework.ReferencePool.Runtime\|FuFramework.TaskPool.Runtime\|FuFramework.Variable.Runtime\|ReferencePoolModule\|VarObject\|VarString" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs`
Expected: 仅 ReferencePool 的注册行。（`BootstrapProcess` 已不使用 Var*/ReferencePool。）

- [ ] **Step 2:** 对三个模块分别执行通用迁移操作 1–6（ReferencePool 有 Editor.asmdef，另两者无）。
- [ ] **Step 3:** `HotfixLauncher` 注册区最前插入 `ModuleManager.RegisterModule<ReferencePoolModule>();`（成为注册序列首位）。TaskPool/Variable 无注册调用。
- [ ] **Step 4:** 确认 `Launcher.Modules.cs` 的 `RegisterModules()` 已清空（9 个注册全部迁走），可保留空方法或删除该文件（若删，同步移除 `Launcher.cs` 中 `Instance.RegisterModules()` 调用——改为下一 Phase 处理，此处先保留空方法）。
- [ ] **Step 5:** Unity 编译 + 进登录界面验证。
- [ ] **Step 6:** `git add -A && git commit -m "refactor: 下沉 ReferencePool/TaskPool/Variable 到 Hotfix 程序集 [AI]"`

---

## Phase 3 · 迁移模块系统本体 + 更新循环移交

### Task 15：迁移 ModuleManager / ModuleBase 到 Hotfix

**Files:**
- Move: `Scripts/FuFramework/Core/Runtime/Framework/ModuleManager.cs` → `Scripts/Hotfix/Framework/Core/ModuleManager.cs`
- Move: `Scripts/FuFramework/Core/Runtime/Framework/ModuleBase.cs` → `Scripts/Hotfix/Framework/Core/ModuleBase.cs`

> `ModuleManager`/`ModuleBase` 命名空间为 `FuFramework.Core.Runtime`，迁入 Hotfix 后**沿用该命名空间**（调用点零改动）。前置：Phase 2 已使 AOT 不再直接调用 `ModuleManager`（更新循环走委托，注册在 HotfixLauncher）。

- [ ] **Step 1: 确认 AOT 侧无 ModuleManager/ModuleBase 引用**

Run: `grep -rn "ModuleManager\|ModuleBase" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs`
Expected: 无（`Launcher.cs` 的委托赋值已改为在 `InvokeHotfixEntryAsync` 中，但那是 static 且将于 Task 17 移交给热更侧；此处确认 AOT 不再 `ModuleManager.RegisterModule` 或 `ModuleManager.Update`）。若 `Launcher.cs` 仍有 `ModuleManager.Update` 临时赋值，记录待 Task 17 处理——为通过本任务编译，先将该临时赋值移除（Phase 1 遗留），委托改由 Task 17 在热更侧赋值。

- [ ] **Step 2: 移动文件**

```bash
git mv "Scripts/FuFramework/Core/Runtime/Framework/ModuleManager.cs" "Scripts/Hotfix/Framework/Core/ModuleManager.cs"
git mv "Scripts/FuFramework/Core/Runtime/Framework/ModuleBase.cs" "Scripts/Hotfix/Framework/Core/ModuleBase.cs"
```
（连同 `.meta` 一起移动；若 `Framework` 目录空则删除。）

- [ ] **Step 3: 处理 Launcher.cs 临时委托赋值**

删除 `InvokeHotfixEntryAsync` 中 `OnUpdate = ModuleManager.Update;` 等三行（AOT 已无 ModuleManager）。同时删除 `Instance.RegisterModules()` 调用与空的 `Launcher.Modules.cs`（注册已全在 HotfixLauncher）。`OnUpdate/OnLateUpdate/OnFixedUpdate` 委托保留，等待热更侧赋值。

```bash
git rm "Scripts/FuFramework/Launcher/Runtime/Launcher.Modules.cs"
```

- [ ] **Step 4: Unity 编译验证**

切 Unity 域重载。
Expected: Core 程序集不再含模块系统；Hotfix 编译通过。此时若未移交更新循环，模块不会被驱动——运行验证放到 Task 17。仅确认编译无报错。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor: 迁移 ModuleManager/ModuleBase 到 Hotfix 程序集 [AI]"
```

### Task 16：迁移 GlobalModule 到 Hotfix

**Files:**
- Move: `Scripts/FuFramework/Launcher/Runtime/GlobalModule.cs` → `Scripts/Hotfix/Framework/Core/GlobalModule.cs`

> 沿用命名空间 `FuFramework.Launcher.Runtime`（调用点 `GlobalModule.UIModule` 等零改动）。

- [ ] **Step 1: 确认 AOT 侧无 GlobalModule 引用**

Run: `grep -rn "GlobalModule" Scripts/AOT Scripts/FuFramework/Core Scripts/FuFramework/ModuleSetting Scripts/FuFramework/Launcher --include=*.cs`
Expected: 无（引导已改用 `BootstrapAssetHelper`/`BootstrapView`）。

- [ ] **Step 2: 移动文件**

```bash
git mv "Scripts/FuFramework/Launcher/Runtime/GlobalModule.cs" "Scripts/Hotfix/Framework/Core/GlobalModule.cs"
```

- [ ] **Step 3: Unity 编译验证**
Expected: 无报错。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "refactor: 迁移 GlobalModule 到 Hotfix 程序集 [AI]"
```

### Task 17：更新循环移交给热更侧模块系统

**Files:**
- Modify: `Scripts/Hotfix/HotfixLauncher.cs`

- [ ] **Step 1: 在 HotfixLauncher 注册完模块后接管帧驱动**

在 `MainAsync` 中所有 `RegisterModule` 之后、`LoadConfigAsync` 之前，插入把 `ModuleManager` 帧驱动赋给 `Launcher` 委托：

```csharp
            // 移交帧驱动：AOT Launcher 的 Update 委托指向热更侧 ModuleManager
            FuFramework.Launcher.Runtime.Launcher.OnUpdate      = ModuleManager.Update;
            FuFramework.Launcher.Runtime.Launcher.OnLateUpdate  = ModuleManager.LateUpdate;
            FuFramework.Launcher.Runtime.Launcher.OnFixedUpdate = ModuleManager.FixedUpdate;
```

（`Launcher` 类在 AOT `FuFramework.Launcher.Runtime`，Hotfix 引用 Launcher 程序集合法。）

- [ ] **Step 2: 清理 AOT.asmdef / Hotfix.asmdef 残留引用**

在 Unity 中打开 AOT.asmdef 与 Hotfix.asmdef 的 Inspector，确认已无任何指向已迁移模块（UI/Asset/Timer/Mono/Event/Procedure/FSM/ObjectPool/ReferencePool/TaskPool/Variable）的 References。AOT.asmdef 仅应保留 `FuFramework.Core.Runtime`、`FuFramework.ModuleSetting.Runtime`、FairyGUI、YooAsset、HybridCLR、UniTask 等引导必需项。

- [ ] **Step 3: Unity 编译 + 完整运行验证（关键回归）**

进入 Play 模式（`EditorSimulateMode`）。
Expected: 加载界面→登录界面全流程正常；模块帧更新正常（如计时器、UI 交互、动画）；Console 无报错。

- [ ] **Step 4: 出包链路验证**

执行 HybridCLR 出包链路：重新生成 `AOTGenericReferences`/`link.xml`（`FuFramework/Build` 或 HybridCLR 菜单），`Copy AOT Code`、`Copy Hotfix Code`，构建一次离线包或真机包，验证 DLL 加载与移交、进入登录界面。
Expected: 真机/离线包正常进入游戏，热更 DLL 加载成功。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor: 更新循环移交热更侧 ModuleManager，完成模块系统下沉 [AI]"
```

---

## Phase 4 · 收尾与文档

### Task 18：清理空目录与更新文档

**Files:**
- Delete: `Scripts/FuFramework/{ReferencePool,ObjectPool,FSM,Procedure,Event,Mono,Timer,Assets,UI,TaskPool,Variable}/` 残留空目录与 `.meta`
- Modify: `CLAUDE.md`、`Docs/项目架构与技术栈.md`

- [ ] **Step 1: 清理已迁移模块的空目录**

确认各已迁移模块目录仅剩空 `Runtime` 目录或空目录后删除（Editor 目录若仍有编辑器代码则保留）。

Run: `find Scripts/FuFramework -type d -empty`
逐一 `git rm -r` 空目录（及其 `.meta`）。

- [ ] **Step 2: 更新架构文档**

修订以下过时描述：
- `CLAUDE.md`：模块系统章节中 "继承自 MonoBehaviour" 改为 "纯 C# 类，由 ModuleManager 反射创建并驱动"。
- `Docs/项目架构与技术栈.md`：AOT/热更分离章节，更新为「AOT 仅含极简启动器（BootstrapProcess/BootstrapAssetHelper/BootstrapView）+ Core 基础工具 + ModuleSetting；模块系统本体与全部框架模块在 Hotfix 程序集」，并补充模块归属表。
- 移除文档中对已删除 `ProcedureXxx` 流程链、`WinLauncher` 的引用，替换为新引导流程说明。

- [ ] **Step 3: 校验文档准确性**

通读修订后的文档，确认与最终代码一致（模块归属、启动流程、命名空间约定）。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs: 更新架构文档以反映框架模块下沉热更后的结构 [AI]"
```

---

## 自检清单（实现完成后运行）

- [ ] `Scripts/AOT/` 不含任何 `FuFramework.<模块>.Runtime`（除 Core/ModuleSetting）的 `using` 或引用。
- [ ] `AOT.asmdef` References 仅含 Core、ModuleSetting、FairyGUI、YooAsset、HybridCLR、UniTask 等引导必需项。
- [ ] `Hotfix.asmdef` References 不含任何已下沉模块（它们已内化到 Hotfix 内部）。
- [ ] `HotfixLauncher.MainAsync` 注册序列为：ReferencePool→ObjectPool→FSM→Procedure→Event→Mono→Timer→Asset→UI→（原 12 个热更模块）。
- [ ] 迁移的模块命名空间保持 `FuFramework.*.Runtime` 不变。
- [ ] 编辑器 `EditorSimulateMode` 与真机/离线包均能进入登录界面。
