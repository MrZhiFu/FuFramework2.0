using FairyGUI;
using GameFrameX.UI.Runtime;
using GameFrameX.Runtime;

#region ReSharper disable
// ReSharper disable CheckNamespace
// ReSharper disable MemberCanBePrivate.Global
#endregion 

namespace Game.Runtime.Logics.UI.View
{
    public partial class #COMPNAME# : IViewCompBase
    {
        /// <summary>
        /// UI组件初始化
        /// </summary>
        public void Init(ViewBase view)
        {
            Log.Info($"初始化{view.UIName}界面组件-{GetType().Name}");
            uiView = view;
            InitUIComp();
            InitUIEvent();
            InitEvent();
        }

        /// <summary>
        /// 注册相关逻辑事件
        /// </summary>
        public void InitEvent()
        {
             // Example:Subscribe(XxxEventArgs.EventId, XxxEventArgs.Create(xxx));
        }
        
        #region UI组件事件处理
    #HANDLER#
        #endregion
    }
}
